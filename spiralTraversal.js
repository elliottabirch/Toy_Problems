function spiralTraversal(matrix) {
  const results = [];

  function createResults(matrix) {
    // NEED BASE CASE!!!!
    // var first and last to save first and last indices of all rows
    // splice first index from matrix and add to results
    // iterate through matrix,
      // splice first last indices from every array and save
    // add lastIndeces array to results
    // add reverse of last array of matrix to results
    // add firstIndeces array to results
  }
}
