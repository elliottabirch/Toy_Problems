let multiply = function (x, y) {
  let total = 0;
  for (let i = 0; i < y; i++) {
    total += x;
  }
  return total;
};

let divide = function (x, y) {

};

let modulo = function (x, y) {
  var remainder = x;
  for (var i = x; )
};
